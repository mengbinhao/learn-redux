const defaultState = {
  placeHolder: 'Pls input something...',
  items: [
    '今天天气好好心情不错哦',
    'React + Redux + React-Router',
    '老汉敲背精油按摩全套打折促销'
  ]
}

export default (state = defaultState, action) => {
  return state
}
